function love.conf(t) 
    t.identity = "Prëss"                    -- The name of the save directory (string)
    
    t.window.title = "Prëss"         -- The window title (string)
    t.window.icon = "/sprites/a2.png"                 -- Filepath to an image to use as the window's icon (string)
    t.window.width = 800                -- The window width (number)
    t.window.height = 600               -- The window height (number)
end